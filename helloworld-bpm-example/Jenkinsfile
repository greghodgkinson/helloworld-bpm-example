pipeline {

    agent {
    
    	label 'buildconductor-bpm'
    //    docker { 
    //      image 'greghodgkinson/jenkins-buildconductor-bpm'                 
    //    }
    }
    
    stages {     
        
        stage('Build') {
            steps {
             
            	sh '/buildconductor/bpm/run-automation-bpm.sh assembleApp HW 0.55 Main 10.0.10.166 20005 ghodgkinson ghodgkinson Staging ghodgkinson ghodgkinson ${BUILD_NUMBER} ${BUILD_URL} ${NODE_NAME}'               
            }
        }
        
        stage('Publish to UCD') {
        	steps {
        	
        	sh "echo Publish to UCD comes next..."               
            }                        	 	
        }
    }
}